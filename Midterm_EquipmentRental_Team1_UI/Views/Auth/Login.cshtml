@if (User.Identity?.IsAuthenticated ?? false)
{
    <form method="post" asp-action="Logout" style="display:inline">
        <button type="submit">Logout</button>
    </form>
}
else
{
    <a asp-action="DoLogin">Login with Google</a>
}